:javascript
  var map = new crowdy.map;
  $(document).ready(map.setup);
  $(window).load(map.setup);

%h1
  = I18n.t 'influenza.title'

%div{class: :waiting, id: :waiting}
  = I18n.t('hazard.search_location')
%div{id: 'map-canvas'}

%p
  = I18n.t 'influenza.part1'

%p
  = I18n.t 'influenza.part2'

